/**
 * Self-update, uninstall, and version check for ccbox.
 *
 * Re-exports from the original upgrade module.
 * Commands directory organization (ARC-11).
 */

export { selfUpdate, selfUninstall, showVersion } from "../upgrade.js";
